# Artefacts de build Rust (core/, wasm/, server/)
target/

# Build wasm-pack (régénéré par `wasm-pack build`, ou par le stage
# wasm-builder du Dockerfile) : pas la peine de le versionner.
wasm/pkg/

# macOS
.DS_Store

# Journal de bord de développement, garde des détails internes (y compris
# des échanges avec l'utilisateur) pas destinés à un dépôt public. Reste
# local uniquement, jamais poussé.
CLAUDE.md
